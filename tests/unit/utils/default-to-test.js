import defaultTo from 'dummy/utils/default-to';
import { module, test } from 'qunit';

module('Unit | Utility | default to');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = defaultTo();
  assert.ok(result);
});
