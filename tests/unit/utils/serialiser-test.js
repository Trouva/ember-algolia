import serialiser from 'dummy/utils/serialiser';
import { module, test } from 'qunit';

module('Unit | Utility | serialiser');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = serialiser();
  assert.ok(result);
});
